/**
 * Front page
 */

;(function (w, d, undefined) {

  window.onload = function () {
    var initBtn = d.querySelector('#initBtn');

    initBtn.addEventListener('click', initVideo, false);

    function initVideo() {
      
    }
  };

})(window, document, undefined);
